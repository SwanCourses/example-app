/**
 * Created by alex on 13.10.16.
 */

// Initial State
const initialState = { };

const UserReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

/* Selectors */

// Export Reducer
export default UserReducer;
